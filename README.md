# ₿

## Deploy previews

Pull requests automatically build and deploy a preview of the site using GitHub
Pages. If the workflow fails due to environment protection rules, make sure the
`preview` environment allows deployments from pull request branches.
